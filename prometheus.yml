global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'spring_micrometer'
    metrics_path: '/actuator/prometheus'
    scrape_interval: 5s
    static_configs:
      - targets: [ 'app1:8081', 'app1:8082'] # требуется прописать ip в /etc/hosts